/* eslint-disable @typescript-eslint/no-explicit-any */
import React from 'react';
import Icon from '@ant-design/icons';

const CustomIcon = ({ component }: any) => <Icon component={component} />;

export default CustomIcon;
